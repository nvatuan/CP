#include <bits/stdc++.h>
#define FOR(i, a, b) for(i = a; i <=b; i++)
#define BAC(i, a, b) for(i = a; i >=b; i--)
using namespace std;

struct house{
	int id;
	int xcoor, ycoor;
	bool powered;
}

int main(){
	//	with n <= 500, brute force is more than enough
	//  to solve this problem
	//  ... but
	//  where is the interest in doing that
	vector<house> vec;
}